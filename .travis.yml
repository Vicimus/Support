language: php
php:
  - '7.1'
  - '7.2'

dist: xenial

before_script:
 - composer update --no-interaction


script:
  - vendor/bin/phpcs "src" "tests" --standard=vendor/vicimus/standard/VicimusStandard --report=full
  - vendor/bin/phpunit --no-coverage
